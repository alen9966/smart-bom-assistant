# 更新日志

## v1.33 网页版 · 2026-08-28

- 「外协阻容备料清单」改为从采购清单中的电阻、电容导出：与外购判定一致，自制阻容不会进入外协表；勾选后按生产数量生成公司外协阻容备料表。

## v1.32 网页版 · 2026-08-27

- 同时上传多个 BOM 时，文件条自动换行，不再横向撑开与右侧项目信息重叠。
- 采购清单「印制板」版号改为从各 BOM 文件名自动识别（如 `ZC7.820.0198_V2_子板_BOM.xlsx` → `ZC7.820.0198-V2`），一般只需填写板厂；页面「PCB版号」仅在文件名无法识别时作为兜底。分别生成时每个 BOM 用本文件版号，合并汇总时每个版号追加一行。

## v1.31 网页版 · 2026-08-24

- 采购清单「印制板」分类改为：页面「PCB版号」和「PCB板厂」都填写后才追加；只填其中一项时不出现该分类。

## v1.30 网页版 · 2026-08-24

- 采购清单自动追加「印制板」分类：名称使用页面「PCB版号」，生产厂家使用页面「PCB板厂」；单机数量为 1，装机/采购总数等于生产数量。BOM 里已有印制板时不会重复追加，但会套用页面板厂。

## v1.29 网页版 · 2026-08-24

- 修复「名称」「型号」等无分隔符的长文本换行后被裁切、看起来像丢信息的问题：按列宽对连续文字硬换行，行高估算更保守，装配清单「名称」同步左对齐。

## v1.28 网页版 · 2026-08-20

- 修复生成表格打印出现大量空白页的问题：未使用的模板空行自动隐藏，打印区域收束到实际数据（有表尾的清单保留表尾），不再把整页空行打出来。

## v1.27 网页版 · 2026-08-20

- 装配清单进一步收窄「位号」列、加宽「备注」列，并启用 A4「适合页宽」打印，避免备注打印时消失。
- 采购 / 外协阻容再次压缩「名称、型号」等列，给备注列更多宽度。

## v1.26 网页版 · 2026-08-20

- 修复采购清单（及外协阻容备料清单）打印超出 A4 导致「备注」列被裁切消失的问题：收窄「名称、型号」等列宽、补全备注列宽度，并启用「按页宽适应」横向打印。

## v1.25 网页版 · 2026-08-20

- 装配清单「型号」「位号」改为左对齐；采购清单与外协阻容备料清单的「名称、型号」同步左对齐，长文本更易阅读。

## v1.24 网页版 · 2026-08-20

- 修复装配清单、采购清单、外协阻容备料清单中长文本（尤其位号）因 Excel 固定行高/未自动换行导致换行后内容被裁切、看起来像漏器件的问题。
- 导出时按分隔符在位号等字段中智能换行，并按内容自动加高对应行；同时为相关单元格样式启用 wrapText。
- 汇总类清单的位号汇总不再按 220 字截断，避免程序侧先丢掉后半段位号。

## v1.23 网页版 · 2026-08-20

- 装配清单「领料数」改为 Excel 公式：`单机数 × 本次装配数量（G3）`。打开导出表后只需改顶部「本次装配数量」，各行领料数会自动重算；导出时仍写入当前计算结果作为缓存值，便于直接查看。
- 同步内置采购清单公司默认模板（以最新 `default-procurement.xlsx` 为准）。

## v1.22 网页版 · 2026-08-17

- 装配变量（Variant）改为「页面提示 + 手动输入」兜底：原始 BOM 没有该列时，页面黄色提醒不会阻止生成，同时在「项目信息」卡片新增单独的装配变量输入框；列值为空、但输入框填了值的行，会使用输入框值；两者都未填时才按「整机采购」处理。
- 修正「页面修改项目工号/产品型号/装配变量等项目信息，但生成的装配清单对应位置没有变化」的问题：元数据统一写入模板各自的“值合并块”左上角（C1 产品型号 / C2 生成时间 / G2 装配变量 / C3 项目工号·名称 / G3 本次装配数量），不再填到标签合并块内部，避免 Excel 合并规则吞掉写入值。
- 装配清单模板按公司最新 v17 版对齐：新增「领料数量」列（总数量 = 单机数量 × 生产数量），厂家、封装、备注列依次后移一位。
- 装配清单模板顶部格式与公司 v17 版式一致，保留字体、字号、边框、合并单元格、打印标题与分页预览设置。
- 领料单归入「其他报表」按需勾选，不再作为常用清单默认勾选。
- 项目输入框「装配变量」改动后立即重算预览行及装配清单顶部的汇总值，所见即所得。

## v1.21 网页版 · 2026-08-17

- 导入的 BOM 如包含“装配变量（Variant）”列，优先使用该列值；如该列值为空，则自动采用“项目信息”中手动填写的装配变量。
- 原始 BOM 未包含装配变量列时，页面会以黄色提醒，并在“项目信息”卡片新增单独的「装配变量」输入框支持手动填写；两者都未填写时按“整机采购”处理。
- 装配变量值不再阻止生成，改为提示 + 手动输入兜底，避免因旧 BOM 缺列无法使用。
- 装配变量自动写入装配清单模板顶部的“装配变量”右侧单元格（G2 合并块）。
- 领料单不再作为常用清单默认勾选，用户需要时可在“其他报表”中手动勾选。
- 装配清单模板升级：新增“领料数量”列（总数量 = 单机数量 × 生产数量），厂家、封装、备注列顺序相应后移。
- 装配清单模板顶部单元格对齐 v17 版式：产品型号、时间、装配变量、项目工号/项目名称、本次装配数量分别写入各自的“值合并块”，不再填到标签合并块内。
- 修正项目信息页面修改产品型号、项目工号等字段后，装配清单对应位置没有变化的问题（根因是值填在错误的合并单元格内，实际显示的是模板自带示例值）。
- 装配清单模板使用用户提供的 v17 版本，保留字体、字号、边框、合并单元格和打印设置。

## v1.20 网页版 · 2026-08-13

- 修复网页版“更改”保存位置按钮被禁用、点击无反应的问题。
- 在最新版 Edge/Chrome 中可选择本地文件夹，生成的 Excel 会直接写入该文件夹。
- 浏览器不支持文件夹选择时会明确提示，并继续使用系统下载目录。
- 启动脚本优先调用 Edge，以提高本地网页版文件夹选择能力的兼容性。

## v1.19 网页版 · 2026-08-13

- “缺少的表头”升级为“模板列对照”：按已勾选清单的当前实际模板，完整列出全部数据列。
- 每列区分“已识别、可推导、自动填写、可留空、缺少”，同时显示对应清单和原表来源。
- 模板不使用的物料编码、单位、厂家料号等字段不再被笼统列为外协清单要求。
- 名称和封装已从 Value、Class、位号等数据成功推导时显示为“可推导”，不再误报缺失。
- 只有数量或关键物料标识确实无法生成时才计入红色缺失项。

## v1.18 网页版 · 2026-08-13

- 面向安装绿盾的公司电脑改为纯网页版交付，由公司默认浏览器进程读取料单。
- 交付 ZIP 不包含 Electron 或其他 EXE，避免未获绿盾透明解密授权的进程直接读取文件。
- 双击 `启动料单助手.bat` 即可在默认浏览器打开；所有程序文件仍在本机离线运行。
- 继续保留 V1.17 的真实 GBK CSV、中文旧版 XLS 和非标准 GBK-XML XLSX 读取修复。

## v1.17 · 2026-08-13

- 修复桌面 EXE 导入部分 ERP、AD 或旧系统生成的非标准 XLSX 时中文乱码。
- XLSX 自动模式始终比较标准 UTF-8、GBK 和 Big5 三种解析结果，避免局部乱码未达到旧阈值时漏检。
- 增加浏览器运行环境的真实 GBK CSV、旧版 XLS、非标准 GBK-XML XLSX 回归测试。
- 启动时验证 CP936 中文代码页实际存在，而不只是检查脚本文件是否加载。

## v1.16 · 2026-08-13

- “需要的表头”改为只显示当前已勾选清单中缺少的字段。
- 已识别字段不再显示；未勾选清单专用的字段也不会提示。
- 清单勾选和字段对应变化后，缺失提示会立即刷新。

## v1.15 · 2026-08-13

- 新增 Windows 桌面版，可打包为本地 `.exe`，无需浏览器打开网页。
- 桌面版可自行选择 Excel 保存目录，并自动记住上次选择的位置。
- 多个输出清单直接写入指定文件夹；完成后显示保存数量和完整路径。

## v1.14 · 2026-08-13

- 修复 v1.13 发布包缺少 `vendor` 目录，导致 Excel 运行库未加载、导入时报 `undefined.read` 的问题。
- 增加发布包自动构建与完整性检查；打包前检查网页引用，打包后检查全部必需文件，避免再次遗漏运行依赖。
- 页面启动时主动检查 Excel、ZIP 和业务模块；依赖缺失时显示明确的“安装包不完整”提示。

## v1.13 · 2026-08-12

- 按用户要求彻底取消业务清单 ZIP 打包：勾选多个清单时依次直接下载多个 `.xlsx`。
- 单个 BOM 勾选四个常用清单时，直接得到装配、领料、采购、外协阻容四个 Excel。
- 多个 BOM 分别生成时，每个输出文件增加 BOM 原文件名前缀，避免不同 BOM 的同类清单互相覆盖。
- 多个 BOM 合并汇总时，每类勾选清单分别下载一个 Excel，不再生成 ZIP。
- 每个下载任务间隔 250ms 触发，降低浏览器漏掉连续下载的概率。
- 浏览器首次弹出“允许下载多个文件”时需要选择允许。

## v1.12 · 2026-08-12

- 多清单 ZIP 改用 STORE 模式，不再对已经压缩的 XLSX 二次 DEFLATE，提升 Windows 资源管理器兼容性。
- ZIP 生成后逐个解压内部文件并与原始 Excel 逐字节比对，文件长度或内容不一致时禁止下载。
- 下载使用的 Blob URL 不再在 60 秒后定时释放，避免慢速硬盘、网络重定向目录或杀毒扫描导致 ZIP 尾部被截断。
- 增加确定性 ZIP 测试，同一批文件重复打包必须得到完全一致的字节。
- 使用 Windows 自带 Expand-Archive 成功解压四个中文文件名清单，并逐个在 Microsoft Excel 中打开；无外链、无修复日志。

## v1.11 · 2026-08-12

- 将用户提供的《ZCJY260704采购清单-0806D-春哥确认阻容》内置为外协阻容备料清单公司默认模板。
- 外协阻容备料清单提升为常用输出，与装配、领料、采购并列默认勾选，可单独指定模板。
- 完整保留原模板的 16 列布局、字体、字号、边框、加粗电容/电阻分类行、分页预览和打印设置。
- F 列单机数量按相同物料汇总原数量；J/K 列装机总数和采购总数按“单机数量 × 生产数量”计算。
- 保留第 92 行研制方式复选项和第 93 行提出人、日期、部门领导、签收人栏。
- 真实 64 行 BOM 在 Microsoft Excel 中验证为 4 页，无外链、无修复提示。

## v1.10 · 2026-08-12

- 领料单分类行改用模板中真正的宋体 9 号粗体样式。
- 领料单每条物料的备注固定填写红色粗体“电装”。
- 领料单、采购清单恢复分页预览，打印区域按实际数据行数自动扩展。
- 领料单、采购清单增加居中页码“第 &P 页，共 &N 页”，并重复打印前三行表头。
- 修复采购清单第一行表尾被误当作数据清空的问题，保留“新研/重复研制”复选项和签字栏。
- 装配清单保持 v1.9 的生成逻辑不变。

## v1.9 · 2026-08-12

- 新增“需要的表头”检查区，已识别字段显示绿色，缺失字段显示红色。
- 每个缺失表头都会说明对装配、领料、采购等清单的具体影响。
- 检查内容随当前导入模式、勾选输出和手动字段对应实时更新。
- 继续保留 v1.8 的多文件 ZIP 完整性校验和安全下载修复。

## v1.8 · 2026-08-12

- 修复同时导出多个 Excel 时，部分 Windows 电脑提示 ZIP 损坏的问题。
- ZIP 改为完整字节生成；下载前自动校验文件头、中央目录、文件数量和 CRC。
- 下载地址保留 60 秒，避免大文件还没有写完就被浏览器提前释放。
- 压缩包中的重复文件名自动添加序号，避免同名清单相互覆盖。
- 主界面右上角增加“更新日志”入口。

## v1.7 · 2026-08-12

- 从常用阻容型号推导装配清单名称和封装。
- 领料单、采购清单增加原模板样式的加粗分类行。
- 清除失效计算链和外部链接，消除 Excel 更新链接、已修复及黑灰打开区域。
- 修复装配清单数据较多时单元格 XML 顺序错误的问题。

## v1.6 及更早

- 内置装配、领料、采购公司默认模板，保留字体、字号、边框、列宽和打印设置。
- 支持多个 BOM、自动表头识别、乱码编码修正，以及分 BOM 或合并导出。
